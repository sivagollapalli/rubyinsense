%legend Add a question
= simple_form_for @question, url: questions_path, defaults: {input_html: { class: "form-control"}} do |f|
  = render :partial => 'form', :locals => { :f => f }
  %br
  = f.submit 'Create', class: 'btn btn-info'
